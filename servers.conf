# Per-server RStudio port for run_geomicro.sh.
#
# Format: hostname:port   (one per line; hostname must match `hostname -s` on that server)
# Add a line here when running this script on a new server - no script changes needed.
# A host not listed here falls back to the default port below.

default:4787

alpena:UPDATE_TO_YOUR_PORT
cayman:UPDATE_TO_YOUR_PORT
vondamm:UPDATE_TO_YOUR_PORT
